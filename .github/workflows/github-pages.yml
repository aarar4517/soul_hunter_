name: Upload Game for Public Download

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  upload-public-game:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Upload Game Files
        uses: actions/upload-artifact@v4
        with:
          name: 🎮 Game Download
          path: |
            *.exe
            *.pck
            *.x86_64
            *.dmg
            Build/
            export/
